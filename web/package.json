{
  "name": "opportunity-static",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:config": "node scripts/generate-firebase-config.js",
    "config": "node scripts/generate-firebase-config.js",
    "setup": "node scripts/generate-firebase-config.js"
  }
}
